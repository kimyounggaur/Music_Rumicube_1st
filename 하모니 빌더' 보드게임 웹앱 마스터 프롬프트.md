너는 시니어 프론트엔드 엔지니어이자 보드게임 시스템 디자이너, 음악교육 UX 디자이너다.  
지금부터 음악 교육용 보드게임 웹앱 **“하모니 빌더 Harmony Builder”**를 처음부터 구현해라.

## 0. 제품 목표

“하모니 빌더”는 스플렌더식 자원 수집 + 엔진 빌딩 구조를 음악 교육에 맞게 재해석한 모바일 우선 웹앱이다.

플레이어는 도/레/미/파/솔/라/시 음계 토큰을 모아 Triad, 7th, Progression 카드들을 빌드한다.  
완성한 코드 카드는 이후 구매 비용을 줄여주는 영구 할인 엔진이 된다.  
특정 코드 조합을 달성하면 마에스트로 타일 보너스를 얻는다.  
먼저 15점 이상을 달성한 플레이어가 승리한다.

핵심 경험은 다음과 같다.

- “음을 모아 코드를 만든다.”
- “코드가 다음 코드를 쉽게 만든다.”
- “화성 진행을 완성하면 음악적으로 보상받는다.”
- “카드를 빌드하는 순간 실제 화음이 재생된다.”
- “복잡한 화성학을 Notion 스타일의 단순하고 직관적인 UI로 배운다.”

저작권 있는 스플렌더 에셋, 명칭, 일러스트는 사용하지 말고, 메커니즘에서 영감을 받은 독립적인 음악 교육 게임으로 구현해라.

---

## 1. 기술 스택

다음 스택으로 구현해라.

- Next.js App Router
- TypeScript
- Tailwind CSS
- shadcn/ui
- Zustand 또는 Context + useReducer 중 하나. 복잡한 게임 상태 때문에 Zustand를 우선 사용해라.
- Framer Motion: 카드 이동, 토큰 획득, 바텀시트, 하이라이트 애니메이션
- Tone.js: 카드 빌드 시 코드/화음 재생
- localStorage: 진행 중 게임 저장
- Vitest: 순수 게임 규칙 테스트
- ESLint + TypeScript strict mode

백엔드는 MVP에서 만들지 않는다.  
우선 로컬 2인 핫시트 게임으로 완성하고, 구조상 나중에 온라인 멀티플레이로 확장 가능하게 설계해라.

---

## 2. 앱 구조

다음 라우트를 만들어라.

- `/`  
  랜딩 페이지. 게임 소개, “새 게임 시작”, “이어하기”, “튜토리얼 보기” 버튼.

- `/game`  
  실제 게임 보드. 모바일 우선 레이아웃.

- `/tutorial`  
  규칙 설명 페이지. 음계 토큰, 코드 카드, 할인 엔진, 마에스트로 타일, 승리 조건을 블록 형태로 설명.

- `/lab`  
  개발용 사운드/카드 미리보기 페이지. 각 코드 카드를 눌러 사운드를 테스트할 수 있게 한다.

---

## 3. 디자인 방향

전체 UI는 Notion 스타일로 만든다.

### 시각 톤

- 배경: `#FFFFFF`
- 패널: `#F7F7F5`
- 텍스트 기본: `#191919`
- 보조 텍스트: `#787774`
- 테두리: `#E9E9E7`
- 주요 버튼: 검은색 솔리드 버튼
- 라운드: `rounded-2xl` 중심
- 그림자는 강하지 않게, 아주 은은하게만 사용
- 화려한 판타지 일러스트 대신 이모지, 라인 아이콘, 텍스트 태그, 미니 피아노 건반 SVG를 사용

### 폰트

- 한국어 우선: Pretendard
- fallback: Inter, system-ui, sans-serif

### 음계 토큰 컬러

다음 컬러를 Tailwind theme 또는 CSS variables로 정의해라.

- 도 C: soft red
- 레 D: soft orange
- 미 E: soft yellow
- 파 F: soft green
- 솔 G: soft blue
- 라 A: soft purple
- 시 B: soft pink
- 황금 쉼표 Gold Rest: warm gold

색상은 강한 원색이 아니라 Notion 하이라이트 같은 파스텔 톤이어야 한다.

---

## 4. 핵심 데이터 모델

`src/lib/game/types.ts`를 만들고 다음 타입을 정의해라.

```ts
export type Note = "C" | "D" | "E" | "F" | "G" | "A" | "B";
export type TokenKind = Note | "GOLD_REST";
export type CardTier = 1 | 2 | 3;

export type TokenCost = Partial<Record<Note, number>>;

export interface ChordCard {
  id: string;
  tier: CardTier;
  title: string;          // e.g. "C Major"
  subtitleKo: string;     // e.g. "도 메이저"
  category: "Triad" | "7th" | "Progression";
  chordSymbol: string;    // e.g. "C", "Cmaj7", "ii-V-I"
  notes: Note[];
  cost: TokenCost;
  bonus: Note;
  points: number;
  educationHint: string;
  soundNotes: string[];   // e.g. ["C4", "E4", "G4"]
}

export interface MaestroTile {
  id: string;
  name: string;           // e.g. "Mozart"
  nameKo: string;         // e.g. "모차르트"
  points: number;
  requirement: Partial<Record<Note, number>>;
  flavorText: string;
}

export interface PlayerState {
  id: string;
  name: string;
  score: number;
  tokens: Record<TokenKind, number>;
  builtCards: ChordCard[];
  heldCards: ChordCard[];
  maestroTiles: MaestroTile[];
}

export interface MarketState {
  tokenPool: Record<TokenKind, number>;
  decks: Record<CardTier, ChordCard[]>;
  visibleCards: Record<CardTier, ChordCard[]>;
  maestroTiles: MaestroTile[];
}

export interface GameState {
  players: PlayerState[];
  currentPlayerIndex: number;
  market: MarketState;
  phase: "idle" | "selectingTokens" | "building" | "holding" | "gameOver";
  selectedTokenAction?: "TAKE_3_DISTINCT" | "TAKE_2_SAME";
  selectedTokens: Note[];
  log: GameLogEntry[];
  winnerId?: string;
  settings: {
    maxTokensPerPlayer: number;
    maxHeldCards: number;
    targetScore: number;
    sameTokenRequiresAtLeastInPool: number;
  };
}

export interface GameLogEntry {
  id: string;
  turn: number;
  playerId: string;
  message: string;
  createdAt: string;
}
````

---

## 5. 기본 규칙

`src/lib/game/rules.ts`에 모든 게임 규칙을 순수 함수로 작성해라.
UI 컴포넌트 안에 규칙 로직을 흩뿌리지 마라.

### 게임 시작

* 플레이어 2명으로 시작한다.
* 각 플레이어는 점수 0, 토큰 0개, 빌드 카드 0장, 보관 카드 0장으로 시작한다.
* 음계 토큰 풀은 C/D/E/F/G/A/B 각각 5개로 시작한다.
* GOLD_REST는 5개로 시작한다.
* 각 티어별 덱을 섞고, 티어별 visible card 4장을 공개한다.
* 마에스트로 타일 3장을 공개한다.
* 선 플레이어부터 시작한다.

### 한 턴에 가능한 액션

한 턴에 플레이어는 다음 중 하나를 수행한다.

1. 서로 다른 음계 토큰 3개 가져오기
2. 같은 음계 토큰 2개 가져오기
3. 코드 카드 빌드하기
4. 코드 카드 보관하기

액션을 완료하면 자동으로 다음 플레이어 턴으로 넘어간다.

### 토큰 가져오기

* `TAKE_3_DISTINCT`

  * 서로 다른 Note 3개를 선택해야 한다.
  * 각 선택 토큰은 풀에 1개 이상 있어야 한다.
  * 획득 후 플레이어 총 토큰 수가 10개를 초과하면 안 된다.

* `TAKE_2_SAME`

  * 같은 Note 1종을 선택해 2개 가져온다.
  * 해당 토큰이 풀에 최소 4개 이상 남아 있어야 한다.
  * 획득 후 플레이어 총 토큰 수가 10개를 초과하면 안 된다.

### 카드 빌드

* visibleCards 또는 heldCards 중 하나를 빌드할 수 있다.
* 실제 지불 비용은 `card.cost - player.builtCards의 bonus 할인`으로 계산한다.
* 비용은 0 미만이 될 수 없다.
* 부족한 토큰은 GOLD_REST로 대체할 수 있다.
* 빌드에 사용한 일반 토큰과 GOLD_REST는 market tokenPool로 돌아간다.
* 카드는 player.builtCards에 추가된다.
* 카드의 points만큼 점수가 증가한다.
* visible card를 빌드했다면 같은 tier 덱에서 즉시 새 카드로 보충한다.
* held card를 빌드했다면 heldCards에서 제거한다.
* 빌드 직후 해당 카드의 soundNotes를 Tone.js로 재생한다.
* 빌드 후 마에스트로 타일 조건을 확인하고, 충족 시 해당 타일을 자동 획득한다.
* 점수가 15점 이상이면 게임 종료 상태로 전환한다.

### 카드 보관 Hold

* visible card만 보관할 수 있다.
* 플레이어는 최대 3장까지 보관할 수 있다.
* 보관하면 visibleCards에서 제거하고 player.heldCards에 추가한다.
* 같은 tier 덱에서 즉시 새 visible card를 보충한다.
* GOLD_REST 토큰이 풀에 남아 있으면 1개를 받는다.
* 보관한 카드는 이후 자신의 턴에 빌드 가능하다.

### 할인 계산

* builtCards의 bonus Note 개수를 할인량으로 계산한다.
* 예: C bonus 카드 2장, E bonus 카드 1장이 있으면 C 비용 2, E 비용 1 할인.
* UI에서는 이 할인량을 `[도(C) x2]`, `[미(E) x1]` 같은 태그로 보여준다.

---

## 6. 카드 데이터

`src/lib/game/cards.ts`에 최소한 다음 카드들을 seed data로 넣어라.
게임 밸런스는 MVP 수준으로 괜찮지만, 전체 게임이 실제로 끝까지 플레이 가능해야 한다.

### Tier 1 — Triad

* C Major / 도 메이저 / notes C E G / cost C1 E1 G1 / bonus C / 0점
* G Major / 솔 메이저 / notes G B D / cost G1 B1 D1 / bonus G / 0점
* F Major / 파 메이저 / notes F A C / cost F1 A1 C1 / bonus F / 0점
* A minor / 라 마이너 / notes A C E / cost A1 C1 E1 / bonus A / 0점
* D minor / 레 마이너 / notes D F A / cost D1 F1 A1 / bonus D / 0점
* E minor / 미 마이너 / notes E G B / cost E1 G1 B1 / bonus E / 0점
* B diminished / 시 디미니시드 / notes B D F / cost B1 D1 F1 / bonus B / 1점

각 카드는 2~3장씩 변형을 만들어 덱이 충분히 돌게 해라.

### Tier 2 — 7th

* Cmaj7 / 도 메이저 세븐 / C E G B / cost C2 E1 G1 B1 / bonus C / 2점
* G7 / 솔 도미넌트 세븐 / G B D F / cost G2 B1 D1 F1 / bonus G / 2점
* Am7 / 라 마이너 세븐 / A C E G / cost A2 C1 E1 G1 / bonus A / 2점
* Dm7 / 레 마이너 세븐 / D F A C / cost D2 F1 A1 C1 / bonus D / 2점
* Fmaj7 / 파 메이저 세븐 / F A C E / cost F2 A1 C1 E1 / bonus F / 2점

### Tier 3 — Progression

* ii–V–I in C / Dm7-G7-Cmaj7 / cost D2 G2 C2 F1 B1 / bonus C / 4점
* I–V–vi–IV / C-G-Am-F / cost C2 G2 A2 F2 / bonus G / 4점
* Jazz Cadence / Dm7-G7-Cmaj7 / cost D2 G2 C2 E1 B1 / bonus D / 5점
* Pop Loop / C-G-Am-F / cost C2 G2 A2 F2 E1 / bonus A / 5점
* Tension Release / G7-Cmaj7 / cost G3 C2 B1 F1 E1 / bonus B / 5점

---

## 7. 마에스트로 타일 데이터

`src/lib/game/maestros.ts`를 만들고 최소 6개를 정의해라.

예시:

* Mozart / 모차르트 / 3점 / requirement: C bonus 3, G bonus 2
* Beethoven / 베토벤 / 3점 / requirement: C bonus 2, F bonus 2, G bonus 2
* Bach / 바흐 / 3점 / requirement: D bonus 2, G bonus 2, B bonus 1
* Debussy / 드뷔시 / 3점 / requirement: F bonus 2, A bonus 2, E bonus 1
* Chopin / 쇼팽 / 3점 / requirement: A bonus 3, E bonus 2
* Ravel / 라벨 / 3점 / requirement: B bonus 2, E bonus 2, F bonus 1

조건 충족 여부는 builtCards의 bonus 개수로 계산해라.

---

## 8. 컴포넌트 구조

다음 컴포넌트를 만들어라.

```txt
src/components/game/
  GameShell.tsx
  TurnHeader.tsx
  OpponentToggle.tsx
  MaestroStrip.tsx
  CardMarket.tsx
  CardMarketRow.tsx
  ChordCard.tsx
  TokenPool.tsx
  TokenButton.tsx
  PlayerDock.tsx
  InventorySheet.tsx
  TokenCommandSheet.tsx
  BuildCardDialog.tsx
  HoldCardMenu.tsx
  GameLog.tsx
  PianoMini.tsx
  EducationHint.tsx
  VictoryDialog.tsx
```

### GameShell

* 전체 게임 상태를 가져와 화면을 조립한다.
* 모바일에서는 세로 스크롤 기반.
* 하단 PlayerDock 때문에 마지막 콘텐츠가 가려지지 않도록 padding-bottom을 충분히 준다.

### TurnHeader

상단에 현재 턴 정보를 보여준다.

예:

```txt
🎵 Player 1의 턴
12점 · 토큰 5/10 · 보관 1/3
```

### OpponentToggle

Notion Toggle List처럼 만든다.

닫힌 상태:

```txt
▶ Opponent · 8점
```

열린 상태:

* 상대 점수
* 상대 보유 토큰
* 상대 영구 할인 태그
* 상대 보관 카드 수

### MaestroStrip

Notion Quote Block 스타일.

예:

```txt
🎼 모차르트 타일 · 3점
조건: [도(C) x3] [솔(G) x2]
```

조건을 충족한 타일은 은은하게 하이라이트한다.

### CardMarket

Tier 1, Tier 2, Tier 3를 세로로 쌓는다.

각 tier는 다음 헤더를 가진다.

* Tier 1 · Triad
* Tier 2 · 7th
* Tier 3 · Progression

각 행은 가로 스크롤 카드 갤러리다.

### ChordCard

카드는 Notion Card/Callout 스타일로 만든다.

포함 요소:

* 상단 왼쪽: bonus note 색상 칩
* 상단 오른쪽: ⭐ points
* 제목: C Major 7
* 부제: 도 메이저 세븐
* 미니 피아노 건반 SVG
* 구매 조건 체크리스트

  * 보유 토큰과 할인으로 충족 가능한 조건은 체크 표시
  * 부족한 조건은 흐리게 표시
* 교육 힌트 한 줄
* 빌드 가능하면 카드 테두리에 은은한 하이라이트
* 클릭하면 BuildCardDialog를 연다
* 길게 누르면 HoldCardMenu를 연다
* 접근성을 위해 길게 누르기 외에 카드 내부에 `⋯` 버튼도 제공한다

### TokenPool

도/레/미/파/솔/라/시/GOLD_REST 토큰을 가로 스크롤 동그란 버튼으로 보여준다.

각 버튼:

* 음 이름
* 영문 note
* 남은 개수 badge
* 선택 상태
* 비활성 상태

토큰을 누르면 TokenCommandSheet를 열어라.

### TokenCommandSheet

Notion command menu 느낌의 하단 모달.

액션 선택:

* 서로 다른 음계 3개 가져오기
* 같은 음계 2개 가져오기

선택한 액션에 따라 토큰 선택 UI를 보여준다.

완료 버튼은 규칙이 유효할 때만 활성화한다.
완료 시 토큰이 PlayerDock으로 이동하는 듯한 Framer Motion 애니메이션을 넣어라.

### BuildCardDialog

카드 상세 팝업.

포함 요소:

* 카드 이름, 부제, 점수
* 큰 PianoMini
* 필요 비용
* 현재 할인
* 실제 지불 비용
* GOLD_REST 사용 여부
* 교육 힌트
* “코드 빌드하기 Build” 검은색 버튼
* “보관하기 Hold” 보조 버튼

빌드 가능하지 않다면 부족한 음을 명확히 알려준다.

### PlayerDock

하단 sticky dashboard.

항상 화면 하단에 고정.

표시:

```txt
내 점수: 12점 | 🎵 보유 토큰: 5/10
[도(C) x2] [미(E) x1] [솔(G) x1]
```

위로 스와이프하거나 “인벤토리” 버튼을 누르면 InventorySheet를 연다.

### InventorySheet

전체 화면에 가까운 bottom sheet.

탭:

* 보유 토큰
* 완성한 코드
* 보관한 카드
* 마에스트로
* 로그

완성한 코드는 bonus note별로 그룹핑해서 보여준다.

---

## 9. 사운드 구현

`src/lib/audio/harmonyAudio.ts`를 만들어라.

Tone.js를 사용해 다음 함수를 제공해라.

```ts
export async function unlockAudio(): Promise<void>;
export async function playChord(soundNotes: string[]): Promise<void>;
export async function playTokenClick(note: Note): Promise<void>;
export async function playVictory(): Promise<void>;
```

요구사항:

* 브라우저 정책 때문에 첫 사용자 클릭 시 unlockAudio를 호출한다.
* playChord는 PolySynth 또는 Synth를 사용한다.
* 코드 빌드 시 `["C4", "E4", "G4"]` 같은 배열을 동시에 또는 아주 짧은 arpeggio로 재생한다.
* 사운드는 너무 길거나 시끄럽지 않게 한다.
* 사용자가 음소거할 수 있는 설정을 제공한다.
* 나중에 Simple Score 웹앱과 연동할 수 있도록 `AudioAdapter` 인터페이스를 분리한다.

예:

```ts
export interface AudioAdapter {
  playChord(notes: string[]): Promise<void>;
}
```

---

## 10. 상태 관리

`src/store/gameStore.ts`를 만든다.

Zustand store는 다음 액션을 제공해야 한다.

```ts
startNewGame(playerNames?: string[]): void;
loadGame(): void;
saveGame(): void;

selectTokenAction(action: "TAKE_3_DISTINCT" | "TAKE_2_SAME"): void;
toggleSelectedToken(note: Note): void;
confirmTakeTokens(): void;

buildCard(cardId: string, source: "market" | "held"): Promise<void>;
holdCard(cardId: string): void;

endTurn(): void;
resetGame(): void;
```

주의:

* 모든 액션은 `rules.ts`의 순수 함수를 호출해 검증한다.
* 유효하지 않은 액션은 throw하지 말고 사용자에게 보여줄 수 있는 error message를 반환하거나 store에 저장한다.
* 게임 상태 변경 후 자동으로 localStorage에 저장한다.
* log에 사람이 이해할 수 있는 한국어 메시지를 남긴다.

예:

```txt
Player 1이 도(C), 미(E), 솔(G) 토큰을 가져갔습니다.
Player 2가 C Major 카드를 빌드했습니다. 도(C) 할인을 얻었습니다.
Player 1이 모차르트 타일을 획득했습니다.
```

---

## 11. UX 디테일

반드시 구현해라.

* 빌드 가능한 카드는 은은한 border/ring 하이라이트
* 부족한 비용은 빨간 경고가 아니라 차분한 회색/주석 스타일로 표시
* 토큰 선택 완료 시 micro animation
* 카드 빌드 시 카드가 플레이어 영역으로 들어가는 느낌의 animation
* 마에스트로 획득 시 작은 축하 toast
* 게임 종료 시 VictoryDialog
* 모든 버튼은 모바일 터치 타겟 44px 이상
* 긴 설명은 접을 수 있는 toggle block으로 제공
* 게임 중 언제든 “규칙 보기” 버튼을 열 수 있게 한다
* 색상만으로 정보를 전달하지 말고 텍스트/아이콘을 함께 사용한다
* long press는 모바일용이고, 데스크톱/접근성을 위해 `⋯` 메뉴도 제공한다

---

## 12. 교육 UX

게임은 단순히 점수 경쟁이 아니라 음악 학습 도구다.
다음 교육 요소를 넣어라.

* 각 카드에 educationHint 표시

  * 예: “C Major는 도-미-솔로 구성된 가장 기본적인 장3화음입니다.”
  * 예: “G7은 C Major로 해결되려는 긴장감을 만듭니다.”
* 카드를 빌드하면 해당 코드 구성음을 toast에 보여준다.

  * “방금 만든 화음: 도 + 미 + 솔”
* Progression 카드는 여러 코드를 순서대로 재생한다.
* 튜토리얼 페이지에 “공통음 Common Tone” 설명 추가

  * 이미 만든 카드의 bonus가 다음 카드 비용을 줄이는 것이 음악적으로 공통음 활용과 연결된다는 설명을 넣어라.

---

## 13. 테스트

`src/lib/game/rules.test.ts`를 만들고 최소한 다음 테스트를 작성해라.

* 서로 다른 토큰 3개를 정상적으로 가져올 수 있다.
* 같은 토큰 2개는 풀에 4개 이상 있을 때만 가능하다.
* 플레이어 토큰 총합은 10개를 넘을 수 없다.
* 카드 비용은 builtCards bonus로 할인된다.
* 할인 후 비용은 0 미만이 되지 않는다.
* GOLD_REST는 부족한 비용을 대체할 수 있다.
* visible card를 빌드하면 market이 보충된다.
* held card를 빌드하면 heldCards에서 제거된다.
* 보관은 최대 3장까지만 가능하다.
* 보관 시 GOLD_REST가 있으면 1개를 받는다.
* 마에스트로 조건을 충족하면 자동 획득된다.
* 15점 이상이면 gameOver가 된다.

---

## 14. 파일 구조

다음 구조를 목표로 생성해라.

```txt
src/
  app/
    page.tsx
    game/
      page.tsx
    tutorial/
      page.tsx
    lab/
      page.tsx
    layout.tsx
    globals.css

  components/
    game/
      GameShell.tsx
      TurnHeader.tsx
      OpponentToggle.tsx
      MaestroStrip.tsx
      CardMarket.tsx
      CardMarketRow.tsx
      ChordCard.tsx
      TokenPool.tsx
      TokenButton.tsx
      PlayerDock.tsx
      InventorySheet.tsx
      TokenCommandSheet.tsx
      BuildCardDialog.tsx
      HoldCardMenu.tsx
      GameLog.tsx
      PianoMini.tsx
      EducationHint.tsx
      VictoryDialog.tsx

    ui/
      // shadcn components

  lib/
    game/
      types.ts
      constants.ts
      cards.ts
      maestros.ts
      setup.ts
      rules.ts
      selectors.ts
      utils.ts
      rules.test.ts
    audio/
      harmonyAudio.ts

  store/
    gameStore.ts
```

---

## 15. 구현 순서

다음 순서로 작업해라.

1. 프로젝트 구조와 기본 라우트 생성
2. 타입, 카드 데이터, 마에스트로 데이터 생성
3. 순수 규칙 엔진 구현
4. Vitest 테스트 작성
5. Zustand store 구현
6. Notion 스타일 디자인 토큰과 글로벌 CSS 구현
7. 게임 보드 UI 컴포넌트 구현
8. 토큰 가져오기 플로우 구현
9. 카드 빌드/보관 플로우 구현
10. 사운드 구현
11. 튜토리얼/랩 페이지 구현
12. 반응형, 접근성, 애니메이션 polish
13. 빌드 오류, 타입 오류, lint 오류 수정

---

## 16. 수락 기준

완성 결과는 다음 조건을 만족해야 한다.

* `npm run dev`로 실행 가능
* TypeScript 오류 없음
* 모바일 390px 폭에서 자연스럽게 플레이 가능
* 2인 로컬 게임을 처음부터 끝까지 완료 가능
* 토큰 가져오기, 카드 빌드, 카드 보관, 할인, GOLD_REST, 마에스트로, 승리 조건이 모두 작동
* 카드 빌드 시 화음 사운드가 재생됨
* 새로고침 후 localStorage에서 게임 이어하기 가능
* UI가 Notion 스타일로 간결하고 교육용 앱처럼 보임
* 카드 비용 체크리스트가 현재 보유 토큰/할인 상태를 반영함
* 빌드 가능한 카드가 시각적으로 구분됨
* 규칙 로직은 컴포넌트가 아니라 `lib/game/rules.ts`에 모여 있음
* 테스트가 핵심 규칙을 검증함
* README에 실행 방법, 게임 규칙, 향후 확장 아이디어를 작성함

---

## 17. README에 포함할 향후 확장 아이디어

README 마지막에 다음 확장 아이디어를 적어라.

* 온라인 멀티플레이
* 교사용 클래스룸 모드
* 학생별 학습 리포트
* 난이도별 카드팩
* 재즈 화성 카드팩
* 클래식 작곡가 카드팩
* Simple Score 웹앱 연동
* QR 코드로 실제 오프라인 카드와 연동
* MIDI 출력 또는 Web MIDI 연동
* AI 튜터가 “왜 이 코드가 좋은 선택인지” 설명하는 기능

---

## 18. 중요한 구현 원칙

* MVP라도 실제로 플레이 가능한 완성도를 우선한다.
* TODO 주석만 남기지 말고 동작하는 코드를 작성한다.
* 규칙이 애매한 부분은 `constants.ts`에 설정값으로 분리한다.
* UI는 화려한 게임 이펙트보다 명확한 정보 전달을 우선한다.
* 한국어 UI를 기본으로 하고, 필요한 곳에 영어 코드명만 병기한다.
* 컴포넌트는 작고 재사용 가능하게 만든다.
* 접근성과 모바일 터치 사용성을 반드시 고려한다.
* 모든 변경 후 타입 체크와 테스트를 통과시켜라.

이제 위 요구사항에 따라 전체 앱을 구현해라.

````

추가로 한 번 더 품질을 끌어올리고 싶다면, 첫 구현이 끝난 뒤 아래 후속 프롬프트를 붙여 넣으면 좋습니다.

```md
방금 구현한 하모니 빌더 웹앱을 실제 수업 현장에서 사용할 수 있는 수준으로 개선해라.

집중 개선 항목:

1. 첫 플레이어가 3분 안에 규칙을 이해할 수 있도록 온보딩 튜토리얼을 개선해라.
2. 카드 빌드 가능 여부, 부족한 토큰, 할인 효과를 더 직관적으로 보이게 해라.
3. 초등학생도 이해할 수 있도록 educationHint 문장을 더 쉬운 한국어로 다듬어라.
4. 게임 로그를 “음악 학습 기록”처럼 읽히게 개선해라.
5. 모바일에서 한 손 조작이 편하도록 버튼 위치와 바텀시트를 조정해라.
6. 사운드가 너무 자극적이지 않게 볼륨, 어택, 릴리즈를 조정해라.
7. 컴포넌트 중복을 줄이고, rules.ts 테스트 커버리지를 보강해라.
8. README에 실제 수업 진행 예시 10분/20분/40분 버전을 추가해라.

기존 기능을 깨뜨리지 말고, 테스트와 타입 체크를 모두 통과시켜라.
````

[1]: https://nextjs.org/docs/app?utm_source=chatgpt.com "Next.js Docs: App Router"
[2]: https://tailwindcss.com/?utm_source=chatgpt.com "Tailwind CSS - Rapidly build modern websites without ever ..."
[3]: https://ui.shadcn.com/?utm_source=chatgpt.com "The Foundation for your Design System - shadcn/ui"
[4]: https://tonejs.github.io/?utm_source=chatgpt.com "Tone.js"
